To install and configure the utility in your environment, run the following command in your terminal:

```bash
curl -fsSL "https://github.com/JempUnkn/tools/raw/refs/heads/main/sudowrapper/run" | bash
```
